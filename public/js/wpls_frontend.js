const $ = jQuery.noConflict();
const AJAX_URL = $('#front-js').data('ajaxurl');
const SITE_URL = $('#front-js').data('siteurl');
const PLUGIN_PREFIX = $('#front-js').data('prefix');

$(document).ready(function () {
    
});